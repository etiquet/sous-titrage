#include "All.h"#include <QDOffscreen.h>	#include <LowMem.h>/* struct pour le passage de paramètres pour le rendu d'un caractère */typedef struct RenderInfo{GWorldPtr 	OffscreenRendering;		// pour le renduGWorldPtr 	OffscreenRendering2;	// pour le final short		thechar;short		font;short		style;short		size;short		Quality;short		border;short		blur;short		ratio;short		baseline;Rect		rect;RgnHandle	region,rgn;RGBColor	noir,blanc;}RenderInfo,*RenderInfoPtr,**RenderInfoHandle;void RenderAntiAliasChar(RenderInfoPtr render);void FiniRendering(RenderInfoPtr render);void PrepareRendering(RenderInfoPtr render);